#include <bits/stdc++.h>
using namespace std;
//function to find the closest char greater than the target
char find_closest_char(vector<char> letters,char target){

}
int main()
{
    vector<char> letters = {"c","f","g"};
    // int size = sizeof(arr) / sizeof(arr[0]);
    cout<<find_closest_char(letters,"a");
    return 0;
}